\documentclass[11pt]{article}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[margin=0.68in]{geometry}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage[table]{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{listings}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{tikz}
\usepackage{titlesec}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}
\graphicspath{{docs/lessons/assets/}{../assets/}}

\definecolor{Ink}{gray}{0}
\definecolor{CodeGray}{gray}{0.96}
\definecolor{Soft}{gray}{0.90}
\hypersetup
{
    pdftitle={ADK Lesson 024: Observable Greenhouse Trainer},
    pdfauthor={Kevin Shanahan},
    pdfsubject={Deterministic moisture, bounded watering intent, presentation, and stable records},
    pdflang={en-US}
}
\pagestyle{fancy}
\fancyhf{}
\lhead{\textsc{ADK Lesson 024}}
\rhead{Observable Greenhouse Trainer}
\cfoot{\thepage}
\setlength{\headheight}{14pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\setlist{nosep,leftmargin=1.45em}
\titleformat{\section}{\Large\bfseries\color{Ink}}{\thesection}{0.5em}{}
\titleformat{\subsection}{\large\bfseries\color{Ink}}{\thesubsection}{0.5em}{}
\lstdefinestyle{adkcpp}
{
    language=C++,
    backgroundcolor=\color{CodeGray},
    basicstyle=\ttfamily\small,
    keywordstyle=\color{Ink}\bfseries,
    commentstyle=\color{Ink},
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false,
    frame=single,
    rulecolor=\color{Ink},
    breaklines=true,
    tabsize=4
}
\newcommand{\checkbox}{\(\square\)}
\newcommand{\blank}[1]{\rule{#1}{0.4pt}}
\newcommand{\lessonbox}[1]{%
  \begin{center}
  \fcolorbox{Ink}{white}{\parbox{0.92\linewidth}{#1}}
  \end{center}}

\begin{document}

\begin{center}
  {\Huge\bfseries Observable Greenhouse Trainer}\\[4pt]
  {\Large Observe, decide, actuate, present, record}\\[8pt]
  \textit{A deterministic project with visible evidence and no energetic load}
\end{center}

\begin{tabularx}{\linewidth}{@{}>{\bfseries}l X >{\bfseries}l X@{}}
Lesson & 024 --- project & Time & 240--330 minutes\\
Prerequisites & Lessons 001--023 & Board & Arduino Mega 2560\\
Evidence & TP-M, D38, D13, RGB, LCD & Status & physical acceptance open\\
Energy class & E1: USB, passive input, LCD, and resistor-limited LEDs only\\
\end{tabularx}

\lessonbox{\textbf{Hard boundary.} ``Pump'' means the LED on D38. Do not
connect a relay, motor, pump, heater, water, external load supply, or mains
circuit. The fixed storage medium is an in-memory teaching model, not an SD
card. ADK is not an unattended irrigation or safety controller.}

\section{Mission and dependency map}

Build one replayable chain:
\[
\text{TP-M}\rightarrow\texttt{MoistureSensor}\rightarrow
\texttt{WateringController}\rightarrow\texttt{PanelPumpOutput}
\rightarrow\texttt{InertLoadPanel}
\]
\[
\hspace{1.5in}\searrow\quad\text{LCD view}\quad+\quad
\text{stable record}\quad+\quad\text{RGB mode evidence}.
\]

\begin{center}
\begin{tikzpicture}[scale=0.91,transform shape,
  node distance=5mm and 7mm,
  box/.style={draw,rounded corners,align=center,minimum height=8mm},
  every path/.style={-{Latex[length=2mm]}}
]
  \node[box] (input) {A0\\\texttt{AnalogInput}};
  \node[box,right=of input] (moisture) {\texttt{MoistureSensor}\\sample};
  \node[box,right=of moisture] (greenhouse) {\texttt{GreenhouseController}\\five stages};
  \node[box,above right=of greenhouse] (watering) {\texttt{WateringController}\\bounded intent};
  \node[box,right=of watering] (pump) {\texttt{PanelPumpOutput}\\pump only};
  \node[box,below right=of greenhouse] (view) {LCD + RGB\\presentation};
  \node[box,below=of greenhouse] (record) {\texttt{RecordSink}\\fixed storage};
  \draw (input) -- (moisture);
  \draw (moisture) -- (greenhouse);
  \draw (greenhouse) -- (watering);
  \draw (watering) -- (pump);
  \draw (greenhouse) -- (view);
  \draw (greenhouse) -- (record);
\end{tikzpicture}
\end{center}

The arrows are ownership or explicit borrowing relationships, not hidden
global access. The coordinator owns dependency lifecycle calls; caller-owned
objects outlive it.

\section{Orientation and exact circuit}

\begin{center}
\includegraphics[width=0.94\linewidth]{024-greenhouse-pencil.png}
\end{center}
\textit{Alternative text: pencil orientation drawing with a Mega 2560, 10
kiloohm potentiometer on A0, 16 by 2 LCD, D40 fan, D38 pump, and D41 heater
indicator LEDs, D13 record LED, and D5--D7 RGB status LED. Every external LED
channel has a resistor and all paths return to common ground. The drawing
contains no relay or load.}

The drawing helps locate parts; this table is authoritative.

\begin{tabularx}{\linewidth}{@{}l l X@{}}
\toprule
Meaning & Pin & Connection\\
\midrule
Moisture simulator / TP-M & A0 / D54 & 10 k\(\Omega\) potentiometer wiper\\
Potentiometer ends & 5 V, GND & one end to each USB-powered rail\\
Simulated fan & D40 & 220 \(\Omega\), LED anode; cathode to GND\\
Simulated pump / TP38 & D38 & 220 \(\Omega\), LED anode; cathode to GND\\
Simulated heater & D41 & 220 \(\Omega\), LED anode; cathode to GND\\
Record acknowledgement & D13 & built-in LED\\
Mode evidence & D5, D6, D7 & RGB channels, one 330 \(\Omega\) resistor each\\
LCD control/data & D30--D35 & RS, E, D4, D5, D6, D7\\
LCD supply & 5 V, GND & exact module rating and contrast network\\
\bottomrule
\end{tabularx}

\textbf{Unpowered inspection.} Remove USB. Confirm the potentiometer endpoints,
one resistor per LED channel, common ground, LCD pin order, and complete absence
of a relay or external supply. Record measured resistances:

\begin{center}
\begin{tabular}{@{}llll@{}}
Potentiometer & \blank{.6in} &
D40/D38/D41 & \blank{.8in}\\
Red resistor & \blank{.6in} &
Green resistor & \blank{.6in}\\
Blue resistor & \blank{.6in} &
Board revision & \blank{1.1in}\\
\end{tabular}
\end{center}

\section{The narrative loop}

\begin{lstlisting}[style=adkcpp]
const adk::TimePoint now (millis ());

observeGreenhouse (now);
decideGreenhouse  (now);
actuateGreenhouse (now);
presentGreenhouse (now);
recordGreenhouse  (now);
showGreenhouseHealth(now);
\end{lstlisting}

\texttt{observe()} samples at most once when due. \texttt{decide()} changes
policy and intent but no output. \texttt{actuate()} is the only stage allowed
to change D38. \texttt{present()} writes an already-decided LCD view.
\texttt{record()} publishes the same decision. Missed periods coalesce; they do
not create a burst.

\subsection{Object reading exercise}

In the canonical sketch, underline each owner once and draw an arrow to every
borrower. Then answer:

\begin{enumerate}
  \item Which object claims A0? \blank{2.2in}
  \item Which object may command D38? \blank{2.2in}
  \item Which object owns the LCD pins? \blank{2.2in}
  \item Why must the record sink outlive the controller? \blank{2.8in}
  \item What runs during exception-driven unwinding in a caller?
        \blank{2.3in}
\end{enumerate}

\section{Watering policy and state graph}

The reference calibration maps raw 800 to dry (0 permille) and raw 200 to wet
(1000 permille). Watering may begin below 350 only after 2000 ms off. It stops
at or above 600, on inhibition, on invalid or stale sensing, or after 5000 ms
on. Maximum-on creates a lockout; another dry sample cannot clear it early.
\texttt{PanelPumpOutput} maps on to the panel's pump channel and off to none.
The fan and heater indicators remain off throughout this project.

\begin{center}
\begin{tikzpicture}[
  node distance=10mm and 14mm,
  state/.style={draw,ellipse,align=center,minimum width=23mm},
  every path/.style={-{Latex[length=2mm]}}
]
  \node[state] (start) {Starting};
  \node[state,right=of start] (idle) {Idle};
  \node[state,right=of idle] (water) {Watering};
  \node[state,below=of water] (lock) {Locked out};
  \node[state,below=of idle] (fault) {Sensor/output\\fault};
  \draw (start) -- node[above]{first decision} (idle);
  \draw[bend left=18] (idle) to node[above]{dry + off time} (water);
  \draw[bend left=18] (water) to node[below]{wet or inhibit} (idle);
  \draw (water) -- node[right]{maximum on} (lock);
  \draw (lock) -- node[below]{minimum off} (idle);
  \draw (idle) -- node[left]{invalid sample} (fault);
  \draw (water) -- node[above,sloped]{invalid/output failure} (fault);
\end{tikzpicture}
\end{center}

\begin{tabularx}{\linewidth}{@{}l X l@{}}
\toprule
Condition & Decision & D38\\
\midrule
Invalid, unavailable, or stale sample & sensor fault dominates & off\\
Operator inhibition & intent forced off & off\\
On-time at or beyond maximum & lockout & off\\
Valid sample at or above wet threshold & stop watering & off\\
Valid sample below dry threshold after off-time & begin watering & on\\
Between thresholds & preserve safe current state & unchanged\\
\bottomrule
\end{tabularx}

\section{Mode precedence and independent evidence}

\begin{tabularx}{\linewidth}{@{}l X X@{}}
\toprule
Mode & RGB pattern & Narrow interpretation\\
\midrule
Starting & slow blue pulse & lifecycle/schedule start\\
Monitoring & green heartbeat & valid sample; pump intent off\\
Watering & cyan heartbeat & D38 requested on\\
Inhibited & steady amber & software policy denied watering\\
SensorFault & two red pulses & sample unusable\\
OutputFault & steady red & command failure latched\\
DisplayFault & three amber pulses & LCD cannot report itself\\
RecordFault & four red pulses & append or sync failed\\
MultipleFaults & steady red & output plus another fault\\
\bottomrule
\end{tabularx}

Output failure dominates and latches until successful reinitialization. Sensor
failure and inhibition always suppress watering. A display or record failure
cannot enable D38.

\subsection{Predict, observe, interpret}

\begin{enumerate}
  \item \textbf{Acquisition.} Predict the starting RGB pattern. Observe the
        LCD startup and RGB transition. Interpret only resource and lifecycle
        progress; do not infer D38 safe state.
  \item \textbf{Safe state.} Predict D38 dark at startup, inhibition, invalid
        sensing, and shutdown. Observe the LED, then measure TP38 relative to
        GND. Interpret commanded level separately from resource acquisition.
  \item \textbf{Record boundary.} Predict D13 changes only after append and
        sync succeed. Observe it at a one-second cadence. Interpret it as
        simulated storage acknowledgement, never SD durability.
\end{enumerate}

\section{Schedule and replay worksheet}

Fill the prediction columns before running:

\begin{tabularx}{\linewidth}{@{}r r c X X X@{}}
\toprule
ms & raw & allowed & mode & D38 & record sequence\\
\midrule
0 & 500 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
1999 & 700 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
2000 & 700 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
4500 & 440 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
6500 & 700 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
8000 & 700 & no & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
16000 & 700 & yes & \blank{.65in} & \blank{.35in} & \blank{.45in}\\
\bottomrule
\end{tabularx}

Replay the exact timestamps, raw values, and inhibition values twice.

\begin{tabularx}{\linewidth}{@{}l X X@{}}
\toprule
Evidence & Run A digest/notes & Run B digest/notes\\
\midrule
Snapshots & \rule{2.1in}{.4pt} & \rule{2.1in}{.4pt}\\
Pump commands & \rule{2.1in}{.4pt} & \rule{2.1in}{.4pt}\\
LCD lines & \rule{2.1in}{.4pt} & \rule{2.1in}{.4pt}\\
Record bytes & \rule{2.1in}{.4pt} & \rule{2.1in}{.4pt}\\
\bottomrule
\end{tabularx}

Conclusion: \checkbox{} equivalent \quad \checkbox{} different.

If different, first divergence: \blank{3.2in}.

\section{Stable records, storage, and time}

Version 1 uses bounded locale-independent ASCII:

\begin{lstlisting}[style=adkcpp]
adk-gh,1,17,42000,valid,472,340,on,dry,watering
\end{lstlisting}

Fields are format, version, sequence, decision milliseconds, sample state, raw
reading, moisture permille or a dash, requested pump, reason, and mode.
Sequence advances only after successful acceptance. One failed pending record
keeps its original timestamp and bytes during retry.

\texttt{FixedStorage} distinguishes staged bytes from a durable prefix:
\texttt{append()} alone is not durability; \texttt{sync()} advances the durable
prefix. Shutdown and restart discard an unsynchronized suffix. This is an
executable storage model in RAM, not evidence for an SD card.

The public \texttt{Rtc} boundary distinguishes valid, unset,
oscillator-stopped, and transport-fault readings. The deterministic
\texttt{SimulatedRtc} returns a fixed epoch plus a sequence. The record sink
requires a valid reading before append and sync. No physical RTC adapter is
selected, so the record schema still uses explicit \texttt{TimePoint}
milliseconds.

\subsection{Record decoding}

Decode one observed record:

\begin{tabularx}{\linewidth}{@{}l X l X@{}}
Sequence & \blank{1.0in} & Decision ms & \blank{1.0in}\\
Sample state & \blank{1.0in} & Raw & \blank{1.0in}\\
Moisture & \blank{1.0in} & Pump & \blank{1.0in}\\
Reason & \blank{1.0in} & Mode & \blank{1.0in}\\
\end{tabularx}

Does the record prove physical media durability?
\checkbox{} yes \quad \checkbox{} no.

Explain: \blank{3.2in}.

\section{Fault matrix and diagnosis}

\begin{tabularx}{\linewidth}{@{}l X X@{}}
\toprule
Injected condition & Required response & Evidence to retain\\
\midrule
Calibration invalid & initialization rejects, no output claim & complete rollback\\
Range or stale sample & D38 off; sensor fault visible & raw reading and state\\
Output command failure & best-effort off; latched output fault & full status\\
LCD write failure & watering state unchanged & RGB display-fault pattern\\
Append failure & pending record retained & original bytes and sequence\\
Sync failure & no durability claim; retry same record & D13 does not acknowledge\\
Delayed stage call & one coalesced operation & timestamp and next snapshot\\
Shutdown in any mode & dependencies close in reverse & TP38 safe state\\
\bottomrule
\end{tabularx}

\begin{center}
\begin{tikzpicture}[
  node distance=6mm,
  q/.style={draw,diamond,aspect=2.4,align=center},
  a/.style={draw,rounded corners,align=center},
  every path/.style={-{Latex[length=2mm]}}
]
  \node[q] (rgb) {RGB fault?};
  \node[a,below left=of rgb] (safe) {Remove USB if wiring, heat,\\or behavior is uncertain};
  \node[q,below right=of rgb] (pump) {D38 on?};
  \node[a,below left=of pump] (sensor) {Compare TP-M raw,\\freshness, and mode};
  \node[q,below right=of pump] (record) {D13 changes?};
  \node[a,below=of record] (host) {Replay host trace; inspect\\append versus sync status};
  \draw (rgb) -- node[above,sloped]{yes} (safe);
  \draw (rgb) -- node[above,sloped]{no} (pump);
  \draw (pump) -- node[above,sloped]{unexpected} (sensor);
  \draw (pump) -- node[above,sloped]{expected} (record);
  \draw (record) -- node[right]{no} (host);
\end{tikzpicture}
\end{center}

Never diagnose by shorting, hot-plugging bare wires, or attaching a real load.

\section{CLI experiment}

\begin{lstlisting}[style=adkcpp]
make host-test
make host-test-sanitize
make arduino-Lesson024GreenhouseTrainer
make size-Lesson024GreenhouseTrainer
make lessons
make lessons-check
make site-check
make upload-Lesson024GreenhouseTrainer PORT=/dev/ttyACM0
make hardware-card LESSON=024 BOARD=mega2560
\end{lstlisting}

Record tool versions: Arduino CLI \blank{1.0in}; AVR core \blank{1.0in};
compiler \blank{1.4in}. Firmware flash \blank{.8in} bytes; static RAM
\blank{.8in} bytes. A compile is not a physical observation.

\section{Exercises}

\begin{enumerate}
  \item Draw the exact precedence path when a record failure and stale sample
        coexist. Which mode appears, and why?
  \item Change only the injected timestamps to cross unsigned rollover.
        Predict each schedule decision before replay.
  \item Inject append failure, make two newer decisions, then retry. Prove the
        accepted bytes describe the original pending decision.
  \item Design a \emph{host-only} RTC fake trace containing Valid, NotSet,
        OscillatorStopped, and TransportFault. Do not invent a hardware adapter.
  \item Explain why an RGB ``watering'' pattern plus D38 high still does not
        prove that a physical pump received energy.
\end{enumerate}

\section{Claim--evidence--reasoning}

\begin{tabularx}{\linewidth}{@{}l X X@{}}
\toprule
Claim & Evidence & Reasoning / limitation\\
\midrule
Decisions replay & \rule{1.45in}{.4pt} & \rule{1.45in}{.4pt}\\[8pt]
Invalid sample suppresses D38 & \rule{1.45in}{.4pt} & \rule{1.45in}{.4pt}\\[8pt]
Record retry is identical & \rule{1.45in}{.4pt} & \rule{1.45in}{.4pt}\\[8pt]
Shutdown is safe & \rule{1.45in}{.4pt} & \rule{1.45in}{.4pt}\\[8pt]
\bottomrule
\end{tabularx}

Keep claims narrow: an LED and fixed storage model cannot establish pump
electrical behavior, SD durability, RTC accuracy, water delivery, plant health,
or unattended safety.

\section{Open Mega 2560 acceptance record}

\textbf{Do not complete from memory or compilation output.}

\begin{tabularx}{\linewidth}{@{}l X@{}}
Date, person, commit & \blank{4.1in}\\
Board revision / USB supply & \blank{4.1in}\\
Potentiometer and LCD markings & \blank{4.1in}\\
Meter / logic instrument & \blank{4.1in}\\
Measured 5 V rail & \blank{1.0in} V\\
TP-M at both stops & dry \blank{.8in} V;\quad wet \blank{.8in} V\\
Measured LED resistors & D40/D38/D41 \blank{1.1in}; RGB \blank{1.2in}\\
Acquisition prediction / observation & \blank{4.1in}\\
D38 safe-state prediction / observation & \blank{4.1in}\\
Record acknowledgement observation & \blank{4.1in}\\
Fan/pump/heater mutual exclusion & \blank{4.1in}\\
Inhibition and stale-sample observation & \blank{4.1in}\\
Reset and shutdown TP38 observation & \blank{4.1in}\\
Deviation and interpretation & \blank{4.1in}\\
\end{tabularx}

\textbf{Stop conditions.} Remove USB on a hot part, unstable connection,
voltage outside the rails, unexpected D38 state, LCD pin ambiguity, or any
disagreement between table and breadboard. Software shutdown is not the
power-removal method.

\section{References and next step}

\begin{itemize}
  \item \href{https://github.com/spincyc/adk/tree/main/examples/Lesson024GreenhouseTrainer}
  {Canonical Lesson 024 sketch}
  \item \href{https://github.com/spincyc/adk/blob/main/src/greenhouse_controller.h}
  {Controller interface} and
  \href{https://github.com/spincyc/adk/blob/main/tests/test_greenhouse_controller.cpp}
  {deterministic tests}
  \item \href{https://github.com/spincyc/adk/blob/main/docs/design/LESSONS_022_024.md}
  {Lessons 022--024 design record} and
  \href{https://github.com/spincyc/adk/blob/main/docs/SAFETY_MODEL.md}
  {ADK safety model}
  \item \href{https://docs.arduino.cc/hardware/mega-2560}
  {Arduino Mega 2560 Rev3 documentation}
  \item \href{https://docs.arduino.cc/learn/programming/sd-guide/}
  {Arduino data-logging guide}
\end{itemize}

The HTML reference carries searchable API links and commands. This PDF is the
printable experiment and evidence record. Next, Lesson 025 separates captured
infrared timing evidence from decoded command policy.

\end{document}
