\documentclass[11pt]{article}
\usepackage[margin=0.68in]{geometry}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{float}
\usepackage[hidelinks]{hyperref}
\usepackage{listings}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{tikz}
\usepackage{titlesec}
\usepackage[table]{xcolor}
\usetikzlibrary{arrows.meta,decorations.pathmorphing,positioning}

\definecolor{CodeGray}{gray}{0.96}
\hypersetup{
  pdftitle={ADK Lesson 015: Build an Environmental Station},
  pdfauthor={Kevin Shanahan},
  pdfsubject={A staged Arduino Mega climate station with LCD and RGB feedback},
  pdflang={en-US}
}
\pagestyle{fancy}
\fancyhf{}
\lhead{\textsc{ADK Lesson 015}}
\rhead{Environmental Station}
\cfoot{\thepage}
\setlength{\headheight}{14pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\setlist{nosep,leftmargin=1.45em}
\titleformat{\section}{\Large\bfseries}{\thesection}{0.5em}{}
\lstdefinestyle{adkcpp}{
  language=C++,
  backgroundcolor=\color{CodeGray},
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  frame=single,
  breaklines=true
}
\newcommand{\safetybox}[1]{\begin{center}\fbox{\parbox{0.92\linewidth}{%
  \textbf{Build safely.} #1}}\end{center}}
\newcommand{\winbox}[1]{\begin{center}\fbox{\parbox{0.88\linewidth}{%
  \textbf{You unlocked:} #1}}\end{center}}

\begin{document}

\begin{center}
  {\Huge\bfseries Build an Environmental Station}\\[4pt]
  {\Large Sensor light \(\rightarrow\) hello screen \(\rightarrow\) live station
  \(\rightarrow\) fault and recovery}\\[7pt]
  \textit{Keep each win. Add only the wires that unlock the next one.}
\end{center}

\begin{tabularx}{\linewidth}{@{}>{\bfseries}l X >{\bfseries}l X@{}}
Lesson & 015 --- project & Time & about 75 minutes\\
You need & Lessons 013 and 014 & Board & Arduino Mega 2560\\
Visible story & D13, LCD, RGB & Power & USB only\\
Finish & two-minute rest, then RESET replays everything\\
\end{tabularx}

\safetybox{Unplug USB before every wiring change. This project conditionally
uses two independently identified parts: a labeled 5 V DHT11 module and a
parallel 16-pin LCD1602 whose pins are documented as VSS, VDD, VO, RS, RW, E,
D0--D7, A, K. A retail family listing does not establish either pin order.
Leave LCD A and K open unless the exact backlight rating and resistor are
known. Use a common-cathode RGB LED with one 330 \(\Omega\) resistor per die.
Stop for heat, odor, resets, or a wire that disagrees with a printed label.}

\section{The four-win plan}

Start from the climate light in lesson 013 and the LCD wiring knowledge from
lesson 014. The final sketch does the quiet engineering work, but the build
feels like a sequence of visible rewards:

\begin{enumerate}
  \item the existing DHT11 and RGB light produce a valid climate color;
  \item D13 flashes, then the correctly wired LCD says
        \texttt{STATION STARTING};
  \item the first complete sensor frame becomes a numbered LCD record and the
        RGB color reacts to humidity;
  \item lifting DATA while unpowered creates a red fault rhythm; restoring it
        lets the next good frame recover automatically.
\end{enumerate}

After two minutes the screen says \texttt{STATION RESTING}, then every owned
pin is released. Press RESET to replay the whole story. There is no worksheet
or terminal ritual between those wins.

\section{Plate 1 --- Find every Mega socket}

\begin{figure}[H]
\centering
% ADK visual: pencil
\begin{tikzpicture}[
    x=0.76cm,y=0.70cm,line width=0.55pt,
    decorate,decoration={random steps,segment length=3pt,amplitude=0.18pt}]
  \draw[rounded corners] (0,0) rectangle (14,6);
  \node[font=\large] at (6.8,3.0) {Arduino Mega 2560 --- top view};
  \draw (1.2,5.15) rectangle (8.9,5.75);
  \foreach \x/\lab in {4.7/7,5.15/6,5.6/5,6.05/4,6.5/3,6.95/2}
    {\draw (\x,5.45) circle (0.09); \node[below,font=\tiny] at (\x,5.32) {\lab};}
  \draw (11.4,0.7) rectangle (12.65,5.35);
  \foreach \y/\a/\b in {5.0/22/23,4.45/24/25,3.9/26/27,3.35/28/29,
                         2.8/30/31,2.25/32/33,1.7/34/35}
    {\draw (11.75,\y) circle (0.09); \draw (12.3,\y) circle (0.09);
     \node[left,font=\tiny] at (11.58,\y) {\a};
     \node[right,font=\tiny] at (12.47,\y) {\b};}
  \draw (3.0,0.4) rectangle (8.8,0.85);
  \foreach \x/\lab in {5.0/3.3V,5.7/5V,6.45/GND,7.25/GND,8.05/VIN}
    {\draw (\x,0.64) circle (0.10); \node[below,font=\tiny] at (\x,0.42) {\lab};}
  \draw[->] (15.1,5.35) -- (6.05,5.45);
  \node[right,font=\small] at (15.1,5.35) {D5--D7: RGB};
  \draw[->] (15.1,4.35) -- (12.28,5.0);
  \node[right,font=\small] at (15.1,4.35) {D22: DHT DATA};
  \draw[->] (15.1,3.25) -- (11.75,2.8);
  \draw[->] (15.1,3.25) -- (12.3,2.8);
  \draw[->] (15.1,3.25) -- (11.75,2.25);
  \draw[->] (15.1,3.25) -- (12.3,2.25);
  \draw[->] (15.1,3.25) -- (11.75,1.7);
  \draw[->] (15.1,3.25) -- (12.3,1.7);
  \node[right,font=\small] at (15.1,3.25) {D30--D35: LCD};
  \draw[->] (15.1,1.0) -- (6.45,0.64);
  \node[right,font=\small] at (15.1,1.0) {labeled 5V and GND};
\end{tikzpicture}
\caption{Mega locator plate. D22 and D30--D35 are sockets in the paired
vertical digital header; D5--D7 are in the top digital header. D13 is the
built-in acquisition LED.}
\end{figure}

Touch each printed socket label with USB unplugged. In particular, do not
mistake D22 for the top-header socket marked 2.

\section{Plate 2 --- Keep the working climate light}

\begin{figure}[H]
\centering
% ADK visual: pencil
\begin{tikzpicture}[
    x=0.57cm,y=0.52cm,line width=0.5pt,
    decorate,decoration={random steps,segment length=3pt,amplitude=0.18pt}]
  \draw[rounded corners] (0,0) rectangle (25,11);
  \draw (0.8,10.3) -- (24.2,10.3);
  \draw (0.8,9.7) -- (24.2,9.7);
  \node[left,font=\scriptsize] at (0.8,10.3) {\(+\)};
  \node[left,font=\scriptsize] at (0.8,9.7) {\(-\)};
  \foreach \y/\l in {8.5/A,7.9/B,7.3/C,6.7/D,6.1/E,4.9/F,4.3/G,3.7/H,3.1/I,2.5/J}
    {\node[left,font=\tiny] at (0.8,\y) {\l};
     \foreach \x in {1,...,24} {\draw (\x,\y) circle (0.052);}}
  \draw[double distance=5pt] (0.8,5.5) -- (24.2,5.5);
  \foreach \x/\n in {7/8,8/9,9/10,17/18,18/19,19/20,20/21}
    {\node[font=\tiny] at (\x,9.15) {\n};}
  \draw[rounded corners] (6.2,6.35) rectangle (10.6,8.9);
  \node[font=\scriptsize] at (8.4,8.62) {identified DHT11 module};
  \foreach \x/\lab in {7/+,8/S,9/-}
    {\draw (\x,6.35) -- (\x,6.1); \node[below,font=\tiny] at (\x,6.02) {\lab};}
  \draw (7,6.1) -- (7,10.3);
  \draw (9,6.1) -- (9,9.7);
  \draw (8,6.1) -- (8,5.82) -- (3.4,5.82)
    node[left,font=\scriptsize]{to Mega D22};
  \draw[rounded corners] (16.3,6.4) arc[start angle=180,end angle=0,
    x radius=2.2,y radius=1.75] -- (20.7,6.4);
  \foreach \x/\lab in {17/R,18/C,19/G,20/B}
    {\draw (\x,6.4) -- (\x,6.1); \node[below,font=\tiny] at (\x,6.02) {\lab};}
  \draw (18,6.1) -- (18,9.7);
  \foreach \x/\pin in {17/5,19/6,20/7}
    {\draw (\x,6.1) -- (\x,5.85) -- (\x-.18,5.65) -- (\x+.18,5.35)
      -- (\x-.18,5.05) -- (\x,4.85) -- (\x,2.5);
     \node[rotate=90,font=\tiny] at (\x+.34,5.5) {330 \(\Omega\)};
     \draw (\x,2.5) -- (\x,1.55) node[below,font=\scriptsize]{D\pin};}
  \node[anchor=east,font=\scriptsize] at (24.2,10.55) {to Mega 5V};
  \node[anchor=east,font=\scriptsize] at (24.2,9.45) {to Mega GND};
\end{tikzpicture}
\caption{Literal first breadboard. Columns run left to right; A--E and F--J
are separate five-hole groups. The module's \emph{named} \(+\), S/DATA, and
\(-\) leads land at E8--E10 even if its physical lead order requires short
re-routing jumpers. The identified common-cathode RGB legs land at E18--E21.}
\end{figure}

\begin{tabularx}{\linewidth}{@{}l l X@{}}
\toprule
From & To & Purpose\\
\midrule
Mega 5V / GND & breadboard \(+\) / \(-\) rails & one supply and return\\
module \(+\), DATA, \(-\) & E8 to \(+\), E9 to D22, E10 to \(-\) &
route by printed names, not guessed order\\
RGB red, green, blue & E18/E20/E21 through 330 \(\Omega\) to D5/D6/D7 &
three bounded color channels\\
RGB common cathode & E19 to \(-\) rail & common return\\
\bottomrule
\end{tabularx}

Replay lesson 013's climate-light sketch now. The RGB light should settle into
a climate color after a valid frame. Keep USB unplugged after that win while
you add the LCD; upload the lesson 015 project only after Plate 3 is complete.

\winbox{the DHT11 can produce a complete frame and the RGB LED can show it.}

\section{Plate 3 --- Add the LCD on the right half}

\begin{figure}[H]
\centering
% ADK visual: pencil
\begin{tikzpicture}[
    x=0.55cm,y=0.55cm,line width=0.5pt,
    decorate,decoration={random steps,segment length=3pt,amplitude=0.18pt}]
  \draw[rounded corners] (0,0) rectangle (28,11);
  \foreach \y/\l in {8.4/A,7.8/B,7.2/C,6.6/D,6.0/E,4.8/F,4.2/G,3.6/H,3.0/I,2.4/J}
    {\node[left,font=\tiny] at (0.7,\y) {\l};
     \foreach \x in {1,...,27} {\draw (\x,\y) circle (0.05);}}
  \draw[double distance=5pt] (0.7,5.4) -- (27.4,5.4);
  \draw (0.7,10.2) -- (13.6,10.2);
  \draw (14.4,10.2) -- (27.4,10.2);
  \draw (0.7,9.6) -- (13.6,9.6);
  \draw (14.4,9.6) -- (27.4,9.6);
  \draw[dashed] (13.4,10.2) arc[start angle=180,end angle=0,
    x radius=0.6,y radius=0.42];
  \draw[dashed] (13.4,9.6) arc[start angle=180,end angle=0,
    x radius=0.6,y radius=-0.42];
  \node[font=\tiny] at (14,8.9) {bridge only if rail is split};
  \node[left,font=\scriptsize] at (0.7,10.2) {\(+\)};
  \node[left,font=\scriptsize] at (0.7,9.6) {\(-\)};
  \foreach \x/\n in {1/31,5/35,10/40,15/45,20/50,25/55,27/57}
    {\node[font=\tiny] at (\x,9.1) {\n};}
  \draw[rounded corners] (4.0,8.65) rectangle (24.0,10.95);
  \node[font=\scriptsize] at (14,10.65) {LCD1602 facing you; identified pin 1 at left};
  \foreach \x/\lab in {5/1,6/2,7/3,8/4,9/5,10/6,11/7,12/8,
                        13/9,14/10,15/11,16/12,17/13,18/14,19/15,20/16}
    {\draw (\x,8.65) -- (\x,8.4); \node[below,font=\tiny] at (\x,8.35) {\lab};}
  \draw (5,8.4) -- (5,9.6);
  \draw (6,8.4) -- (6,10.2);
  \draw (9,8.4) -- (9,9.6);
  \foreach \x/\pin in {8/30,10/31,15/32,16/33,17/34,18/35}
    {\draw (\x,8.4) -- (\x,7.9) -- (\x,6.0) node[below,font=\tiny]{Mega D\pin};}
  \foreach \x in {11,12,13,14,19,20}
    {\draw[very thick] (\x,8.35) -- (\x,7.95);
     \node[below,font=\tiny,rotate=90] at (\x,7.85) {open};}
  \draw[rounded corners] (3.2,1.3) rectangle (8.8,4.7);
  \node[font=\scriptsize] at (6,4.35) {10 k\(\Omega\) contrast pot};
  \draw (4.4,1.3) -- (4.4,0.8) -- (2.0,0.8) -- (2.0,9.6);
  \draw (7.6,1.3) -- (7.6,0.8) -- (10.0,0.8) -- (10.0,10.2);
  \draw (6,1.3) -- (6,0.4) -- (7,0.4) -- (7,8.4);
  \node[anchor=east,font=\scriptsize] at (27.2,10.45) {to Mega 5V};
  \node[anchor=east,font=\scriptsize] at (27.2,9.35) {to Mega GND};
\end{tikzpicture}
\caption{Literal close-up of columns 31--57 on the \emph{same} 830-point
breadboard. LCD pins 1, 2, 3, 4, 5, 6, and 11--14 connect; pins 7--10 remain
open in four-bit mode; pins 15--16 remain open because this lesson makes no
backlight assumption. The contrast potentiometer ends reach the rails and its
center wiper reaches LCD pin 3. Dashed jumpers bridge a rail split only when
an unpowered continuity check finds one.}
\end{figure}

\begin{tabularx}{\linewidth}{@{}l l l@{}}
\toprule
LCD pin & Name & Literal connection\\
\midrule
1, 2, 3 & VSS, VDD, VO & GND, 5 V, contrast-pot wiper\\
4, 5, 6 & RS, RW, E & Mega D30, GND, Mega D31\\
7--10 & D0--D3 & leave open\\
11--14 & D4--D7 & Mega D32, D33, D34, D35 in order\\
15--16 & A, K & leave open unless independently qualified later\\
rail split & \(+\) and \(-\) & same-polarity bridges only if continuity shows
a break\\
\bottomrule
\end{tabularx}

Keep the climate light in the left-side columns from Plate 2 and place this LCD
close-up in right-side columns 31--57 of the same 830-point board. With USB
unplugged, check continuity along both power rails. If either rail is split,
fit the two dashed same-polarity bridges shown; never bridge \(+\) to \(-\).
Set contrast near its midpoint. Inspect every numbered path, plug in USB, and
press RESET.

\winbox{D13 flashes, then the LCD says \texttt{STATION STARTING} and
\texttt{WAIT FOR RECORD}. That screen requires all sensor, LCD, RGB, and D13
resources to have been acquired.}

\section{Plate 4 --- Watch the station tell its story}

\begin{figure}[H]
\centering
% ADK visual: pencil
\begin{tikzpicture}[
    x=0.72cm,y=0.72cm,line width=0.55pt,>=Latex,
    decorate,decoration={random steps,segment length=3pt,amplitude=0.18pt}]
  \foreach \x/\top/\bottom in
    {2/{STATION STARTING}/{WAIT FOR RECORD},
     8/{HEALTHY \#000001}/{T+21.50 H0480},
     14/{SENSOR \#000002}/{T+00.00 H0000},
     20/{HEALTHY \#000003}/{T+21.60 H0485}}
  {
    \draw[rounded corners] (\x-2.5,0) rectangle (\x+2.5,3.6);
    \draw (\x-2.1,0.8) rectangle (\x+2.1,2.8);
    \node[font=\ttfamily\scriptsize] at (\x,2.15) {\top};
    \node[font=\ttfamily\scriptsize] at (\x,1.45) {\bottom};
  }
  \draw[->,thick] (4.7,1.8) -- (5.3,1.8);
  \draw[->,thick] (10.7,1.8) -- (11.3,1.8);
  \draw[->,thick] (16.7,1.8) -- (17.3,1.8);
  \node[font=\tiny] at (11,0.35) {unplug, lift DATA, repower};
  \node[font=\tiny] at (17,0.35) {unplug, restore DATA, repower};
\end{tikzpicture}
\caption{Visible state progression. Exact values vary, but success requires a
numbered healthy record and a climate color. Missing DATA produces an
explicit fault instead of counterfeit values; restoring DATA permits a later
healthy record.}
\end{figure}

The RGB LED is part of the story:

\begin{tabularx}{\linewidth}{@{}l X@{}}
\toprule
Light & Meaning\\
\midrule
blue pulse & waiting for a first complete frame\\
steady green & valid humidity below 55\% RH\\
steady cyan & valid humidity from 55\% through 69.9\% RH\\
steady amber & valid humidity at least 70\% RH\\
two red pulses & sensor transaction or validation fault\\
three red pulses & impossible caller time; RESET starts a new bounded run\\
\bottomrule
\end{tabularx}

Cup your hands loosely near the sensor or breathe gently across it from several
inches away. Do not wet or touch the vents. Watch the displayed humidity and
color move together. A complete frame, checksum, range check, and freshness
rule stand between a wire twitch and a healthy record.

For the reversible surprise, unplug USB, lift only the D22-to-DATA jumper, and
repower. The station keeps running but shows a named sensor fault with red
pulses. Unplug, restore that jumper, and repower. The hello, first record, and
climate color return. This proves recovery by producing the cool result, not
by filling out a test card.

\section{What the code protects}

\begin{lstlisting}[style=adkcpp]
const adk::Status                status   = station.update (now);
const adk::EnvironmentalSnapshot evidence = station.snapshot ();

presentEnvironment (evidence);
showHealth (evidence.record.health, now);
\end{lstlisting}

\texttt{EnvironmentalStation} owns the sensor lifecycle and samples only when
its caller time says a record is due. A late loop makes one observation, not a
burst of stale transactions. A record keeps its values, sample state, sensor
status, timestamp, and sequence together. Invalid or missing sensor data stays
explicit; this is a learning instrument, not a calibrated monitor or alarm.

The LCD and RGB LED are separate presentation objects. A blank LCD cannot
rewrite sensor truth, and an old good sample cannot masquerade as a new one.
After 120 seconds, the station displays its rest message for 1.5 seconds, turns
off the indicators, shuts down the LCD and sensor, and releases D13, D5--D7,
D22, and D30--D35. RESET begins another run.

Behind the scenes, automated host checks still replay exact deadlines,
transport faults, stale data, recovery, resource conflicts, rollback,
timestamp rollover, restart, repeated shutdown, and identical traces. Those
checks keep the code sturdy; they are not learner chores.

\section{If a win does not appear}

\begin{tabularx}{\linewidth}{@{}p{0.22\linewidth} X X@{}}
\toprule
What you see & Likely boundary & Next move with USB unplugged\\
\midrule
no D13 flash & acquisition or power & inspect all named pins for duplicates or shorts\\
D13 flash, blank LCD & LCD supply, contrast, or pin order & verify pins 1--6 and 11--14 by number; turn contrast gently\\
garbled text & D4--D7 order & trace LCD 11, 12, 13, 14 to D32, D33, D34, D35\\
hello forever & DHT path & verify named module leads, D22, supply, and documented pull-up\\
record but RGB dark & LED identity or channel path & verify common cathode and all three 330 \(\Omega\) resistors\\
red pulses & missing or rejected sensor frame & restore DATA and shorten/check the identified module wiring\\
plausible but surprising values & sensor limitations or environment & treat them as educational indications, not calibrated facts\\
\bottomrule
\end{tabularx}

\section{Make it yours}

\begin{enumerate}
  \item Change the two starting lines to a family station name, keeping each
        line at 16 printable characters or fewer.
  \item Choose three different low-duty RGB colors for dry, comfortable, and
        humid air, then predict which one appears before RESET.
  \item Add a tiny arrow or face to the second LCD row without hiding the
        record number or the explicit fault word.
  \item Press RESET near the two-minute boundary and watch the complete
        acquisition-to-record story begin again.
\end{enumerate}

\section{Next}

Keep the delight: one correctly connected subsystem unlocks the next visible
state. Lesson 016 changes the input side of that pattern by turning a keypad
matrix into explicit operator events.

\end{document}
