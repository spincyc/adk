\documentclass[11pt]{article}

\usepackage[letterpaper,margin=0.72in,headheight=15pt]{geometry}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage[american]{circuitikz}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\hypersetup
{
    pdftitle={ADK Lesson 001: DigitalOutput},
    pdfauthor={Kevin Shanahan},
    pdfsubject={Safe deterministic digital output on the Arduino Mega 2560},
    pdflang={en-US}
}

\usetikzlibrary{arrows.meta,decorations.pathmorphing,positioning}
\graphicspath{{docs/lessons/assets/}{../assets/}}

\definecolor{ink}{gray}{0}
\definecolor{soft}{gray}{0.94}
\definecolor{mid}{gray}{0.42}
\definecolor{accent}{gray}{0}
\definecolor{codebg}{gray}{0.97}

\tikzset
{
    pencil stroke/.style={
        draw=black!72,
        decorate,
        decoration={random steps,segment length=3pt,amplitude=0.55pt},
        line cap=round,
        line join=round
    },
    pencil diagram/.style={
        every node/.style={text=black!82}
    }
}

\pagestyle{fancy}
\fancyhf{}
\lhead{\textsc{ADK field lesson 001}}
\rhead{DigitalOutput}
\cfoot{\thepage}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\renewcommand{\arraystretch}{1.2}

\lstdefinestyle{adkcpp}
{
    language=C++,
    basicstyle=\ttfamily\small,
    backgroundcolor=\color{codebg},
    frame=single,
    rulecolor=\color{mid},
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false,
    keywordstyle=\bfseries\color{accent},
    commentstyle=\itshape\color{mid},
    breaklines=true,
    tabsize=4
}

\newcommand{\blankline}{\rule{0.96\linewidth}{0.35pt}}
\newcommand{\checkbox}{\(\square\)}

\newenvironment{checkpoint}[1]
{\begin{center}\begin{minipage}{0.94\linewidth}\textbf{Check --- #1}\par}
{\end{minipage}\end{center}}

\begin{document}

\begin{center}
    {\Huge\bfseries Make state visible}\\[3pt]
    {\Large Lesson 001: safe digital output}\\[7pt]
    \textit{Predict $\rightarrow$ claim $\rightarrow$ command $\rightarrow$ release}
\end{center}

\begin{center}
% ADK visual: pencil
\includegraphics[width=\linewidth,height=0.31\textheight,keepaspectratio]
    {001-digital-output-pencil.png}
\end{center}

\textbf{Orientation plate.}
This graphite view helps you find the Mega 2560, USB connector, and built-in
indicator. It is not a pin map. The exact electrical path appears below.

\begin{tabularx}{\linewidth}{@{}>{\bfseries}lX@{}}
Question & Can an owning C++ object make one hardware state visible, then
return its pin to a safe high-impedance state?\\
Time & 50--70 minutes\\
Energy class & E1: one Mega 2560, its factory LED and resistor, USB power, and
the separately identified weak-bias acceptance fixture. Host tests and
unpowered inspection are E0.\\
You need & Arduino Mega 2560, known-good USB data cable, current-limited host
USB port, Arch Linux host, \texttt{arduino-cli}, and this ADK checkout\\
Proof & Independent acquisition status, one short acquisition pulse, three
visible behavior cycles, and the host trace proving the controlled
high-impedance shutdown command\\
New words & digital level, output driver, high impedance, ownership, RAII\\
\end{tabularx}

\section*{Safety boundary}

Use only the board's factory-installed LED and USB power. Work dry, on a
nonconductive surface. Disconnect USB, barrel power, \texttt{VIN}, batteries,
shields, and every lead before inspection. Proceed only when the board and
cable are undamaged and no header pins touch.

This is an E1 lesson. The teaching circuit is the Mega 2560 factory
\texttt{LED\_BUILTIN} path. The optional acceptance configuration adds only a
measured 10\,k\(\Omega\) resistor from D13 to the measured 5\,V rail and a DMM
from D13 to GND. Install and inspect both with USB removed; record resistor
tolerance/rating, meter input resistance/range, and the exact board schematic.
No external supply is admitted. USB removal at the computer end is the
physical stop. Software
shutdown, reset, and an extinguished LED are not emergency stops and do not
prove that the pin is high impedance.

\textbf{Stop immediately} for heat, smoke, odor, unexpected power flicker,
repeated USB disconnects, or a loose connector. Disconnect upstream power.
Do not touch a hot board or retry a damaged one. Label it \textsc{do not use}.

\begin{checkpoint}{all power removed before inspection}
\checkbox\ dry board \quad \checkbox\ clear headers \quad
\checkbox\ sound cable \quad \checkbox\ no other power source
\end{checkpoint}

\begin{center}
% ADK visual: schematic
\begin{circuitikz}[american voltages]
    \draw
        (0,3.0) node[vcc]{measured 5\,V}
        to[R,l={measured 10\,k\(\Omega\)}] (0,1.5)
        -- (3.0,1.5) node[circ,label=above:{D13 / TP-LED}] (d13) {};
    \draw
        (d13) -- (5.0,1.5)
        to[voltmeter] (5.0,0)
        node[ground]{};
    \node[align=left,anchor=west] at (5.7,1.5)
        {DMM red: D13\\DMM black: GND};
\end{circuitikz}
\end{center}

\textbf{Formal acceptance-fixture schematic.}
With USB removed, connect the measured resistor from the measured board 5\,V
rail to D13 and the DMM from D13 to board GND. The ground branch in the drawing
is the meter reference, not a jumper that shorts D13 to ground. The factory LED
path stays on the board; use the schematic for the exact Mega revision to
identify it rather than reconstructing it on the breadboard.

\section*{1. Separate a command from an electrical state}

A digital output actively drives a pin near ground for \texttt{Low} or near
the board supply for \texttt{High}. High impedance, written \texttt{HiZ},
means the output driver is disconnected. It is not another voltage level.
An unconnected high-impedance pin may float.

\begin{center}
% ADK visual: pencil
\begin{tikzpicture}[pencil diagram,>=Latex,node distance=9mm and 14mm,
    box/.style={draw=black!72,rounded corners,fill=soft,minimum width=28mm,
                minimum height=10mm,align=center}]
    \node[box] (object) {\texttt{DigitalOutput}\\owns pin claim};
    \node[box,right=of object] (driver) {Mega output\\driver};
    \node[box,right=of driver] (led) {board LED\\and resistor};
    \node[above=5mm of driver] (supply) {\(V_{\rm CC}\)};
    \node[below=5mm of driver] (ground) {GND};
    \draw[pencil stroke,->,thick] (object)--node[above]{\texttt{write()}}(driver);
    \draw[pencil stroke,->,thick] (driver)--node[above]{pin level}(led);
    \draw[pencil stroke,->] (supply)--(driver);
    \draw[pencil stroke,->] (ground)--(driver);
    \node[below=12mm of object,text width=11.5cm,align=center] {
        Exact lesson path: software object \(\rightarrow\)
        \texttt{LED\_BUILTIN} output driver \(\rightarrow\)
        factory LED circuit. No external wire is added.};
\end{tikzpicture}
\end{center}

\textbf{Predict.} Which state actively sources or sinks current?
\rule{3cm}{0.35pt}\quad Which state releases the pin?
\rule{3cm}{0.35pt}

\section*{2. Read the ownership contract}

ADK treats the pin as a resource, not a global integer. Construction produces
an inert object. \texttt{initialize()} claims the pin and configures it
transactionally. \texttt{write()} changes only an initialized, owned output.
\texttt{shutdown()} is idempotent, selects input mode, and releases the pin as
high impedance.
Destruction calls shutdown, so stack unwinding in an exception-enabled host
still cleans up even though ADK does not throw internally.

\begin{center}
% ADK visual: pencil
\begin{tikzpicture}[pencil diagram,>=Latex,node distance=11mm,
    state/.style={draw=black!72,rounded corners,fill=soft,minimum width=37mm,
                  minimum height=11mm,align=center}]
    \node[state] (inert) {inert\\unclaimed, \texttt{HiZ}};
    \node[state,right=of inert] (active) {active\\claimed, driving};
    \draw[pencil stroke,->,thick] (inert) to[bend left=16]
        node[above]{successful \texttt{initialize()}} (active);
    \draw[pencil stroke,->,thick] (active) to[bend left=16]
        node[below]{\texttt{shutdown()} or destructor} (inert);
    \draw[draw=black!72,->] (inert) edge[loop left] node[left]{failed initialize} (inert);
    \draw[draw=black!72,->] (active) edge[loop right] node[right]{\texttt{write()}} (active);
\end{tikzpicture}
\end{center}

\begin{tabularx}{\linewidth}{@{}lXX@{}}
\toprule
Operation & Success & Failure or repetition\\
\midrule
construct & inert; no hardware access & not applicable\\
\texttt{initialize()} & claim, initial level, output mode & invalid or busy; remain inert\\
\texttt{write(level)} & drive requested level & report status; preserve contract\\
\texttt{shutdown()} & input mode, \texttt{HiZ}, release & repeated call is harmless\\
destruct & performs shutdown & never throws\\
\bottomrule
\end{tabularx}

\section*{3. Inspect the experimental first-class interface}

This interface is host-verified but still experimental; it is not yet a
stable supported release. \texttt{Runtime} owns the resource registry shared
by components. The canonical complete example is
\texttt{Lesson001DigitalOutput.ino} in its matching \texttt{examples}
directory.

\begin{lstlisting}[style=adkcpp]
adk::Runtime       runtime;
adk::DigitalOutput output (runtime.resources (), LED_BUILTIN);

adk::Status status = output.initialize ();
if (!status.ok ())
{
    // Stay inert. Report the explicit status.
}

status = output.write (adk::Level::High);
status = output.write (adk::Level::Low);
output.shutdown ();
\end{lstlisting}

\textbf{Why no implicit setup?} A constructor cannot return a status without
exceptions. Explicit initialization makes an invalid pin or resource conflict
observable. RAII still guarantees cleanup after successful acquisition.

\textbf{Why an explicit initial level?} Setting the output latch before enabling
the output driver avoids a brief unintended pulse. For the board LED, inactive
is low. A generic output's safe electrical endpoint is high impedance.

\section*{4. Build evidence on the host first}

The repository's fake Arduino functions record hardware calls as an ordered
trace. Host tests are fast, deterministic, and require no connected board.
This excerpt follows the assertions in \texttt{tests/test\_io.cpp}.

\begin{lstlisting}[style=adkcpp]
fake::reset ();
adk::ResourceRegistry resources;
{
    adk::DigitalOutput output (resources, 13);

    require (output.initialize ().ok (),
             "output initialization");
    require (output.write (adk::Level::High).ok (),
             "output high write");
}
\end{lstlisting}

The test then checks this exact operation order: write \texttt{LOW}, select
\texttt{OUTPUT}, write \texttt{HIGH}, and finally select \texttt{INPUT} during
destruction. The claim is then available to a replacement object.

\begin{checkpoint}{test the boundaries, not only the happy path}
\checkbox\ initialize success \quad
\checkbox\ duplicate pin claim \quad
\checkbox\ invalid pin \quad
\checkbox\ write before initialize \quad
\checkbox\ repeated shutdown \quad
\checkbox\ destructor while active
\end{checkpoint}

\textbf{Trace exercise.} Write the expected operations when a second output
tries to claim an already-owned pin:

\blankline\\[5pt]\blankline

\section*{5. Compile for the Mega 2560}

From the repository root, run the documented checks and the lesson's Arduino
compile target. Read the command before executing it. Confirm the fully
qualified board name is \texttt{arduino:avr:mega}; never infer a connected
board or port.


The maintained gates retain compile and size evidence outside the learner activity.

\begin{tabularx}{\linewidth}{@{}lX@{}}
Host tests & \rule{0pt}{0.7cm}\\
Mega compile & \rule{0pt}{0.7cm}\\
Flash bytes & \rule{0pt}{0.7cm}\\
Static RAM bytes & \rule{0pt}{0.7cm}\\
\end{tabularx}

\section*{6. Run the board acceptance procedure}

The canonical sketch is a finite, reproducible run. Successful initialization
starts a 250\,ms acquisition pulse, followed by 750\,ms dark separation. Three
behavior cycles then alternate 500\,ms high and 500\,ms low. The sketch calls
\texttt{shutdown()} after the third low interval and makes no further hardware
calls. Pressing reset starts a new run; removing USB leaves the board
unpowered.

\begin{enumerate}
    \item With all power removed, repeat the visual inspection.
    \item Connect only the USB data cable.
    \item Run \texttt{arduino-cli board list}. Copy the Mega's exact port.
    \item Upload the lesson sketch using the repository's documented target.
          Do not paste a port belonging to another device.
    \item Predict the acquisition pulse, separation interval, three cycles,
          and final dark state before upload.
    \item Observe the short acquisition pulse separately from the three slower
          behavior cycles without touching conductors.
    \item Wait for the controlled shutdown after the third cycle. Record the
          LED state, but do not call darkness proof of high impedance.
    \item The host trace must end in \texttt{INPUT}. For separate physical
          evidence, install an identified, measured 10\,k\(\Omega\) weak
          pull-up from TP-D13 to the measured 5\,V rail with USB removed.
          Record the exact board's D13 LED circuit and calculate its effect.
          Driven LOW must remain within the documented LOW limit; after the
          final LOW and shutdown, TP-D13 must enter the calculated weak-bias
          range. If the board circuit or prediction is unresolved, leave this
          stage blank.
    \item Press reset once. Confirm that a new acquisition pulse precedes a
          new behavior run, then wait for shutdown again.
    \item Disconnect USB at the computer end. Confirm that every board
          indicator is unpowered before handling the board.
\end{enumerate}

\paragraph{Physical acceptance boundary.}
Measured bench acceptance remains a separately controlled campaign; this
learner lesson does not ask the reader to create or review repository records.
\section*{7. Debug with the smallest useful signal}

Digital output comes first because one spare pin can expose control flow
without a debugger. Assign meanings before running:

\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
Pattern & Meaning\\
\midrule
steady inactive & waiting or safely stopped\\
one short pulse & update loop reached\\
two short pulses & input event accepted\\
slow repeated pulse & recoverable status\\
fast repeated pulse & stop and inspect\\
\bottomrule
\end{tabular}
\end{center}

Patterns are protocol, not decoration. Keep them deterministic, document the
time base, and test the trace. Never use delay-based debug output in code whose
timing contract forbids blocking.

\section*{8. Deliberately test misuse}

Run these on the host fake. Do not create shorts or faults on hardware.

\begin{enumerate}
    \item Construct two outputs for one pin. The second initialization must
          report a conflict without disturbing the first.
    \item Call \texttt{write()} before initialization. Record its status and
          confirm no platform operation occurred.
    \item Initialize with \texttt{NUM\_DIGITAL\_PINS}. Confirm
          \texttt{error() == StatusCode::InvalidPin}, an empty trace, and no
          resource claim.
    \item Call \texttt{shutdown()} twice. Confirm the second call is harmless.
    \item Leave scope while active. Confirm destructor cleanup matches explicit
          shutdown.
\end{enumerate}

\textbf{Explain.} Why is ``the LED went dark'' weaker evidence than ``the pin
became high impedance''?
\par\blankline\\[5pt]\blankline

\section*{9. Extension: polarity belongs above the endpoint}

\texttt{DigitalOutput} owns an electrical endpoint and speaks
\texttt{High}/\texttt{Low}. A later \texttt{MonoLed} composes it and speaks
\texttt{on}/\texttt{off}, accounting for active-high or active-low wiring.
Do not teach a generic pin that \texttt{High} always means ``on.''

\begin{center}
% ADK visual: pencil
\begin{tikzpicture}[pencil diagram,>=Latex,node distance=13mm,
    box/.style={draw=black!72,rounded corners,fill=soft,minimum width=34mm,
                minimum height=11mm,align=center}]
    \node[box] (app) {project\\semantic intent};
    \node[box,right=of app] (led) {\texttt{MonoLed}\\polarity};
    \node[box,right=of led] (out) {\texttt{DigitalOutput}\\electrical level};
    \draw[pencil stroke,->,thick] (app)--node[above]{on/off}(led);
    \draw[pencil stroke,->,thick] (led)--node[above]{High/Low}(out);
\end{tikzpicture}
\end{center}

\section*{Exit ticket}

\begin{enumerate}
    \item What does the object own after successful initialization?
    \item Why is \texttt{HiZ} not equivalent to \texttt{Low}?
    \item Which operation must be safe to repeat?
    \item How does explicit status reporting coexist with RAII?
    \item Name one host-only fault test and its required final state.
\end{enumerate}

\textbf{Ready for lesson 002 when you can:}
\checkbox\ predict the trace \quad
\checkbox\ explain safe shutdown \quad
\checkbox\ identify pin conflict \quad
\checkbox\ compile for Mega \quad
\checkbox\ separate electrical and semantic state

\vfill
\small
\textbf{Further reading.}
Use the companion HTML API page for exact signatures, source links, current
limitations, and searchable test names. Use this PDF for the guided circuit
experiment and diagnosis.
Arduino's Mega 2560 documentation supplies authoritative board limits; the ADK
safety page supplies the project-wide stop conditions. Links are maintained in
the HTML lesson to avoid stale printed URLs.

\end{document}
