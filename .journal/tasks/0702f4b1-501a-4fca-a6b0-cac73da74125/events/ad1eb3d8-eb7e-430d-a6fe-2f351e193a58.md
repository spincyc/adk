---
schema_version: 1
event_uuid: "ad1eb3d8-eb7e-430d-a6fe-2f351e193a58"
event_type: "review"
scope: "task"
task_ids: ["0702f4b1-501a-4fca-a6b0-cac73da74125"]
agent_instance_uuid: "145d0ab8-c97b-4d75-bfec-9677327b5348"
created_at: "2026-07-27T22:22:45Z"
---

# Independent lesson 029 core review

Parallel independent audits covered lifecycle, plan validation, timing,
same-time ordering, rollover, stop dominance, hold/resume, capacity,
encoding, C++11 portability, and inert-only safety. Confirmed defects were
fixed in the core and focused tests:

- scheduler initialization now owns the audit transaction, rejects an occupied
  ledger, rolls back failure, and preserves records through scheduler shutdown;
- plans accept nonzero first offsets and reject duplicate IDs, overlap,
  half-range ambiguity, and invalid configuration explicitly;
- the run edge establishes the epoch, logical confirmation windows do not move
  with delayed observation, visibility starts at accepted confirmation, and
  delayed skips/completion are preflighted and recorded atomically;
- invalid chords precede review release, cancel remains dominant, held recovery
  requires a fresh run, interrupted cues do not resume, and terminal states
  remain inert;
- snapshots and records distinguish absent cues, completion is recorded, and
  stable encoding computes required size and never partially writes.

The later lesson 030 readiness rule is used for equal timestamps: an identical
frame is idempotent and a changed frame is rejected. This is more specific than
the lesson 029 brief's general statement that repeated timestamps are valid.

Remaining integration work is intentionally retained: register both focused
tests in the normal host matrix, export the reviewed headers through the
umbrella, and prove the real AVR build in task
`d79a95ee-e892-427e-9c6b-7fc151608dc5`. Before lesson 030 implementation,
resolve its explicit cue-to-`InertChannelId` mapping without making lesson 029
depend on lesson 028; the current cue ID remains an opaque display label.
